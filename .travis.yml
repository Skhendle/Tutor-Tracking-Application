dist: xenial
language: python
python:
  - "3.7"
cache: pip
before_install:
 - pip install coveralls
install:
 - pip install -r requirements.txt
script: coverage run -m pytest
after_success:
 - codecov
